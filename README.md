# zrlog-plugin-rss

ZrLog RSS 插件。生成 RSS Feed XML 文件，并可在前台通过小工具显示订阅入口。

## 功能

- 配置 RSS 文件路径
- 配置前台订阅入口说明文字
- 在文章发布或修改后刷新 Feed 文件
- 可将 feed 文件同步到静态资源存储

## 构建

```shell
export JAVA_HOME=${HOME}/dev/graalvm-jdk-latest
export PATH=${JAVA_HOME}/bin:$PATH
```
